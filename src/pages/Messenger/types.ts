import {
    AppBar, SideBar, Dialog, MessageForm,
} from '../../blocks'

export type IMessengerPageProps = {
    abbBar: AppBar;
    sideBar: SideBar;
    dialog: Dialog[];
    messageForm: MessageForm;
};
